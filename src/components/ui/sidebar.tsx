
"use client";

// This component is no longer in use and has been replaced by a top navigation bar
// in `app-shell.tsx`. This file is kept to avoid breaking changes if it was
// referenced elsewhere, but its content can be safely removed.

export {};
